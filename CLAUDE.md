# مشروع أودو CCD — ذاكرة المشروع

هذا الملف يُحمَّل تلقائياً في كل جلسة Claude Code. الغرض منه أن تجد أي جلسة
سحابية جديدة (من أي جهاز) كامل السياق فوراً.

## نظرة عامة

أدوات للاتصال والعمل مع سيرفر أودو الخاص بـ **المطور المعتمد للمقاولات (CCD)**.

- **السيرفر**: `https://erp.ccd.sa`  (IP: 85.190.254.163 — VPS من Contabo)
- **الإصدار**: Odoo 19.0
- **قاعدة البيانات**: `ccd_erp`
- **مستخدم الإدارة**: `admin` (بحروف صغيرة — حسّاس لحالة الأحرف)
- **الشركة**: المطور المعتمد للمقاولات — CCD (عربي، توقيت الرياض)

## كيف تتصل

الاتصال يتم عبر **XML-RPC على HTTPS** (يعمل من بيئة Claude السحابية).
بيانات الاعتماد تُقرأ من ملف `.env` (مستثنى من Git — انسخ `.env.example` واملأه).

```python
from odoo_ccd import connect
odoo = connect()
partners = odoo.search_read("res.partner", [], ["name", "phone"], limit=10)
```

### قيود البيئة السحابية (مهم)
- **SSH محجوب تماماً** من بيئة Claude (المنفذ 22 مغلق + لا يوجد عميل ssh).
  أي مهمة تحتاج الجهاز نفسه (نشر موديول، systemctl، سجلات النظام) تُنفَّذ عبر
  سكربتات `scripts/ssh/` **من جهازك المحلي**.
- المنفذ 8069 محجوب أيضاً — الوصول لأودو يتم عبر النطاق `erp.ccd.sa` على 443 فقط.

## بنية المشروع

```
odoo_ccd/            مكتبة الاتصال (XML-RPC، بلا تبعيات خارجية)
scripts/
  health_check.py    فحص الاتصال والحالة وعدد السجلات
  export_data.py     تصدير أي نموذج إلى CSV
  upsert_records.py  إنشاء/تحديث السجلات من CSV (معاينة افتراضية)
  fix_kanban_card.py إصلاح خطأ Missing 'card' template (معاينة + تراجع)
  ssh/               سكربتات SSH (تُشغَّل من الجهاز المحلي)
addons/ccd_custom/   هيكل موديول مخصص لأودو 19
migrations/          نسخ احتياطية ورقع لتعديلات على السيرفر
```

## سجل العمل المنجز

1. **الاتصال بأودو** عبر XML-RPC — مؤكّد (uid=2، قراءة/كتابة تعمل).
2. **أدوات دائمة**: مكتبة الاتصال + فحص الحالة + تصدير + إنشاء/تحديث السجلات.
3. **إصلاح خطأ `Missing 'card' template`**: أودو 19 يتطلب تسمية قالب kanban بـ
   `card` بدل `kanban-box`/`kanban-card`. أُصلحت 4 عروض من موديولات غير مُرقّاة
   (`om_account_asset`, `om_account_budget`, `aos_construction_management`)
   مباشرة على السيرفر عبر `scripts/fix_kanban_card.py`. نسخ احتياطية في
   `migrations/kanban_card_fix/backup/`. التراجع: `--restore --commit`.

## ملاحظات للاستمرار

- الموديولات الثلاثة أعلاه غير مُرقّاة لأودو 19 وقد تُظهر أخطاء أخرى — شخّصها بنفس
  الطريقة (فحص `ir.ui.view` / السجلات عبر XML-RPC).
- الإصلاحات على مستوى قاعدة البيانات تُمحى إن رُقّي الموديول من ملفاته — الحل
  الدائم إصلاح ملفات الموديول ونشرها عبر SSH من الجهاز المحلي.

## أمان

- لا توضع بيانات اعتماد في Git إطلاقاً — تُقرأ من `.env` المستثنى.
- يُفضّل استخدام API key قابل للإلغاء بدل كلمة مرور الحساب.
- بيانات SSH (root) للجهاز — تُحمى بمفتاح SSH.
